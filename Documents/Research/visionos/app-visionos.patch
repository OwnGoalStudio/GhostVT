--- a/iGhostVT/Backend/Session/LockableTerminalView.swift	2026-08-30 22:09:21
+++ b/iGhostVT/Backend/Session/LockableTerminalView.swift	2026-08-31 12:37:12
@@ -43,8 +43,10 @@
             // an empty keyboard leaves it (and its dictation button) floating
             // over the terminal, stealing 40pt of grid. Empty its groups for
             // as long as the lock lasts.
+            #if !os(visionOS)
             inputAssistantItem.leadingBarButtonGroups = []
             inputAssistantItem.trailingBarButtonGroups = []
+            #endif
             guard isFirstResponder else { return }
             // Already first responder: swap the input views in place, so
             // locking drops the keyboard that is up and unlocking brings it
@@ -66,11 +68,13 @@
         isSoftwareKeyboardLocked ? suppressedInputView : super.inputView
     }
 
+    #if !os(visionOS)
     override var inputAccessoryView: UIView? {
         // The bar belongs to the keyboard; leaving it floating over a
         // keyboard that is not there reads as a half-open keyboard.
         isSoftwareKeyboardLocked ? nil : super.inputAccessoryView
     }
+    #endif
 
     override func hitTest(_ point: CGPoint, with event: UIEvent?) -> UIView? {
         isInteractionLocked ? nil : super.hitTest(point, with: event)
--- a/iGhostVT/Backend/LiveActivity/SessionActivityController.swift	2026-08-30 22:09:21
+++ b/iGhostVT/Backend/LiveActivity/SessionActivityController.swift	2026-08-31 12:37:12
@@ -3,7 +3,9 @@
 //  iGhostVT
 //
 
+#if canImport(ActivityKit)
 import ActivityKit
+#endif
 import Foundation
 
 /// Mirrors the open terminal tabs into one Live Activity, so the Dynamic
@@ -122,7 +124,7 @@
         return path
     }
 
-    #if !targetEnvironment(macCatalyst)
+    #if !targetEnvironment(macCatalyst) && canImport(ActivityKit)
         @available(iOS 16.2, *)
         private static func apply(_ state: TerminalSessionAttributes.ContentState) async {
             let existing = Activity<TerminalSessionAttributes>.activities
--- a/Shared/Activity/TerminalSessionAttributes.swift	2026-08-31 01:20:41
+++ b/Shared/Activity/TerminalSessionAttributes.swift	2026-08-31 12:37:12
@@ -6,7 +6,9 @@
 //  payload contract between the two.
 //
 
+#if canImport(ActivityKit)
 import ActivityKit
+#endif
 import Foundation
 
 /// The ActivityKit conformance is added below, off Catalyst only: the
@@ -62,7 +64,7 @@
     }
 }
 
-#if !targetEnvironment(macCatalyst)
+#if !targetEnvironment(macCatalyst) && canImport(ActivityKit)
     @available(iOS 16.2, *)
     extension TerminalSessionAttributes: ActivityAttributes {}
 #endif
--- a/iGhostVT/Interface/Support/GlassStyle.swift	2026-08-29 02:39:14
+++ b/iGhostVT/Interface/Support/GlassStyle.swift	2026-08-31 12:38:42
@@ -8,17 +8,25 @@
     /// (the sidebar opening) moves the label first and drags the background
     /// a beat behind it. Grouping resolves the subtree's geometry in one
     /// transaction so both travel together. iOS 15/16 have no equivalent.
+    ///
+    /// visionOS has no `glassEffect`; its windows are already glass, so the
+    /// material fallback is the whole treatment there.
     @ViewBuilder
     func barGlass(in shape: some Shape, interactive: Bool = true) -> some View {
-        if #available(iOS 26.0, *) {
-            glassEffect(interactive ? .regular.interactive() : .regular, in: shape)
-                .geometryGroup()
-        } else if #available(iOS 17.0, *) {
+        #if os(visionOS)
             background(.ultraThinMaterial, in: shape)
                 .geometryGroup()
-        } else {
-            background(.ultraThinMaterial, in: shape)
-        }
+        #else
+            if #available(iOS 26.0, *) {
+                glassEffect(interactive ? .regular.interactive() : .regular, in: shape)
+                    .geometryGroup()
+            } else if #available(iOS 17.0, *) {
+                background(.ultraThinMaterial, in: shape)
+                    .geometryGroup()
+            } else {
+                background(.ultraThinMaterial, in: shape)
+            }
+        #endif
     }
 }
 
@@ -29,10 +37,14 @@
     @ViewBuilder var content: () -> Content
 
     var body: some View {
-        if #available(iOS 26.0, *) {
-            GlassEffectContainer(spacing: spacing, content: content)
-        } else {
+        #if os(visionOS)
             content()
-        }
+        #else
+            if #available(iOS 26.0, *) {
+                GlassEffectContainer(spacing: spacing, content: content)
+            } else {
+                content()
+            }
+        #endif
     }
 }
--- a/iGhostVT/Interface/Alert/AlertCardView.swift	2026-08-30 23:42:35
+++ b/iGhostVT/Interface/Alert/AlertCardView.swift	2026-08-31 12:38:55
@@ -221,13 +221,19 @@
     /// Liquid Glass on iOS 26+; the material card it replaces below.
     @ViewBuilder
     func cardGlass(in shape: some Shape) -> some View {
-        if #available(iOS 26.0, *) {
-            glassEffect(.regular, in: shape)
-        } else {
+        #if os(visionOS)
             background(.regularMaterial)
                 .background(Color(UIColor.systemBackground).opacity(0.5))
                 .clipShape(shape)
-        }
+        #else
+            if #available(iOS 26.0, *) {
+                glassEffect(.regular, in: shape)
+            } else {
+                background(.regularMaterial)
+                    .background(Color(UIColor.systemBackground).opacity(0.5))
+                    .clipShape(shape)
+            }
+        #endif
     }
 }
 
--- a/iGhostVT/Interface/Support/KeyboardState.swift	2026-08-30 22:09:21
+++ b/iGhostVT/Interface/Support/KeyboardState.swift	2026-08-31 12:38:18
@@ -40,6 +40,10 @@
     }
 
     private static func isSoftwareKeyboard(_ notification: Notification) -> Bool {
+        #if os(visionOS)
+        // The visionOS keyboard is its own window; the frame says nothing.
+        return true
+        #else
         guard
             let value = notification
             .userInfo?[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
@@ -51,5 +55,6 @@
         // (even the floating one collapsed to a toolbar does not post here)
         // is far taller.
         return visibleHeight > 100
+        #endif
     }
 }
--- a/iGhostVT.xcodeproj/project.pbxproj	2026-08-31 02:37:56
+++ b/iGhostVT.xcodeproj/project.pbxproj	2026-08-31 12:50:19
@@ -566,7 +566,9 @@
 				SUPPORTS_MACCATALYST = YES;
 				SWIFT_EMIT_LOC_STRINGS = YES;
 				SWIFT_VERSION = 6.0;
-				TARGETED_DEVICE_FAMILY = "1,2";
+				TARGETED_DEVICE_FAMILY = "1,2,7";
+				SUPPORTED_PLATFORMS = "iphoneos iphonesimulator macosx xros xrsimulator";
+				XROS_DEPLOYMENT_TARGET = 26.0;
 			};
 			name = Debug;
 		};
@@ -589,7 +591,9 @@
 				SUPPORTS_MACCATALYST = YES;
 				SWIFT_EMIT_LOC_STRINGS = YES;
 				SWIFT_VERSION = 6.0;
-				TARGETED_DEVICE_FAMILY = "1,2";
+				TARGETED_DEVICE_FAMILY = "1,2,7";
+				SUPPORTED_PLATFORMS = "iphoneos iphonesimulator macosx xros xrsimulator";
+				XROS_DEPLOYMENT_TARGET = 26.0;
 			};
 			name = Release;
 		};
@@ -599,7 +603,7 @@
 				CODE_SIGN_STYLE = Automatic;
 				MACOSX_DEPLOYMENT_TARGET = 13.0;
 				PRODUCT_NAME = ighostvtd;
-				SUPPORTED_PLATFORMS = "iphoneos macosx";
+				SUPPORTED_PLATFORMS = "iphoneos macosx xros xrsimulator";
 				SUPPORTS_MACCATALYST = NO;
 				SWIFT_VERSION = 5.0;
 				TARGETED_DEVICE_FAMILY = "1,2";
@@ -612,7 +616,7 @@
 				CODE_SIGN_STYLE = Automatic;
 				MACOSX_DEPLOYMENT_TARGET = 13.0;
 				PRODUCT_NAME = ighostvtd;
-				SUPPORTED_PLATFORMS = "iphoneos macosx";
+				SUPPORTED_PLATFORMS = "iphoneos macosx xros xrsimulator";
 				SUPPORTS_MACCATALYST = NO;
 				SWIFT_VERSION = 5.0;
 				TARGETED_DEVICE_FAMILY = "1,2";
@@ -671,7 +675,7 @@
 				CODE_SIGN_STYLE = Automatic;
 				MACOSX_DEPLOYMENT_TARGET = 13.0;
 				PRODUCT_NAME = "ighostvtd-io";
-				SUPPORTED_PLATFORMS = "iphoneos macosx";
+				SUPPORTED_PLATFORMS = "iphoneos macosx xros xrsimulator";
 				SUPPORTS_MACCATALYST = NO;
 				SWIFT_VERSION = 5.0;
 				TARGETED_DEVICE_FAMILY = "1,2";
@@ -684,7 +688,7 @@
 				CODE_SIGN_STYLE = Automatic;
 				MACOSX_DEPLOYMENT_TARGET = 13.0;
 				PRODUCT_NAME = "ighostvtd-io";
-				SUPPORTED_PLATFORMS = "iphoneos macosx";
+				SUPPORTED_PLATFORMS = "iphoneos macosx xros xrsimulator";
 				SUPPORTS_MACCATALYST = NO;
 				SWIFT_VERSION = 5.0;
 				TARGETED_DEVICE_FAMILY = "1,2";
@@ -697,7 +701,7 @@
 				CODE_SIGN_STYLE = Automatic;
 				MACOSX_DEPLOYMENT_TARGET = 13.0;
 				PRODUCT_NAME = "ighostvt-cli";
-				SUPPORTED_PLATFORMS = "iphoneos macosx";
+				SUPPORTED_PLATFORMS = "iphoneos macosx xros xrsimulator";
 				SUPPORTS_MACCATALYST = NO;
 				SWIFT_VERSION = 5.0;
 				TARGETED_DEVICE_FAMILY = "1,2";
@@ -710,7 +714,7 @@
 				CODE_SIGN_STYLE = Automatic;
 				MACOSX_DEPLOYMENT_TARGET = 13.0;
 				PRODUCT_NAME = "ighostvt-cli";
-				SUPPORTED_PLATFORMS = "iphoneos macosx";
+				SUPPORTED_PLATFORMS = "iphoneos macosx xros xrsimulator";
 				SUPPORTS_MACCATALYST = NO;
 				SWIFT_VERSION = 5.0;
 				TARGETED_DEVICE_FAMILY = "1,2";
@@ -777,13 +781,9 @@
 /* End XCConfigurationList section */
 
 /* Begin XCRemoteSwiftPackageReference section */
-		AA0000000000000000000A01 /* XCRemoteSwiftPackageReference "libghostty-spm" */ = {
-			isa = XCRemoteSwiftPackageReference;
-			repositoryURL = "https://github.com/Lakr233/libghostty-spm";
-			requirement = {
-				kind = upToNextMajorVersion;
-				minimumVersion = 1.4.12;
-			};
+		AA0000000000000000000A01 /* XCLocalSwiftPackageReference "spm-local" */ = {
+			isa = XCLocalSwiftPackageReference;
+			relativePath = "../spm-local";
 		};
 /* End XCRemoteSwiftPackageReference section */
 
